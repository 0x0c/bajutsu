[English](BE-XXXX-worker-app-binary-delivery.md) · **日本語**

# BE-XXXX — アップロードされたアプリバイナリを、ジョブを実行する Worker へ届ける

<!-- BE-METADATA -->
| 項目 | 値 |
|---|---|
| 提案 | [BE-XXXX](BE-XXXX-worker-app-binary-delivery-ja.md) |
| 提案者 | [@paihu](https://github.com/paihu) |
| 状態 | **実装済み** |
| トラッキング Issue | [検索](https://github.com/bajutsu-e2e/bajutsu/issues?q=is%3Aissue+label%3Aroadmap-tracking+in%3Atitle+"BE-XXXX") |
| 実装 PR | [#1933](https://github.com/bajutsu-e2e/bajutsu/pull/1933) |
| トピック | Web UI のホスティング |
<!-- /BE-METADATA -->

## はじめに

ホスティングされた Bajutsu は、1 つのプロセスを 2 つに分けて動きます。Control plane が Web の
ユーザーインタフェース（UI）を提供し、ジョブのキューを持ちます。別プロセスの `bajutsu worker`
が、そのジョブを HTTP 経由でリースします。Worker は、実機やシミュレーターを操作できるマシンの上で
ジョブを走らせます。両者はファイルシステムを共有しません。

利用者は、zip バンドルを Web UI にドロップして設定を開きます。その zip には `bajutsu.config.yaml`
と、シナリオのツリーと、設定の `appPath` が指すビルド済みアプリバイナリを収めています。Compose
ピッカーを使うと、同じ 3 つを個別のアーティファクトとしてアップロードできます。どちらの経路でも、
Control plane はアップロードを自分のディスク上のツリーへ展開してバインドします。

本項目は、バインドされたツリーをジョブのリース先の Worker へ届けます。実行が始まったとき、設定が指す
場所にバイナリが存在する状態を作ります。

## 動機

現在の Worker は、アップロードされたバンドルを実行できません。ジョブ仕様は、設定とシナリオを
`materials` としてテキストで運びます。ジョブ仕様は `app_path` も運びますが、`app_path` は文字列
です。バイナリの実体を運ぶ経路がありません。

そのため、実行は Worker 上に存在しないパスに対して始まります。オンデマンドのビルドも肩代わり
しません。Bajutsu はビルド済みアプリを受け取ってビルドはしないので、`_governed_build` は
アップロードのバインドの `build:` を方針として無効にします。`_build_app` はビルドせず成功を
返します。失敗が表に出るのはインストールの時点で、原因から遠く離れた不明瞭なエラーになります。

アップロードの 2 経路は、どちらも同じ壁に当たります。

| バインドの種類 | Control plane が持つもの | 現在 Worker が受け取るもの |
|---|---|---|
| 単一 zip（BE-0073） | 展開済みツリーと、オブジェクトストレージ上の raw zip | 設定とシナリオのテキストだけ |
| 三つ組（BE-0268） | 合成済みツリーと、オブジェクトストレージ上の各成果物 | 設定とシナリオのテキストだけ |

**確認できる結果**：この変更のあと、アップロードされたバンドルから投入したジョブは、リモートの
Worker 上でアプリを導入して実行できます。アプリの複製を持たない Worker を備えたホスティング環境
で、Web UI から実行を 1 本投入すれば、読み手は違いを確かめられます。

## 詳細設計

設計は、BE-0160 がビジュアルベースライン向けに作った仕組みをそのまま使います。Control plane が
URL に署名し、Worker は自前のクラウド認証情報を持たないまま平文の HTTP で取得します。リース応答に
新しい `bundle_urls` フィールドが加わり、既存の `baseline_urls` の隣に並びます。この
フィールドは、バンドルを構成する保存済みオブジェクトごとに presigned GET URL を 1 本ずつ運びます。

### 単位 1：どちらのバインドでも通じる、1 つの記述

どちらのバインドも、バインドされたツリーを識別する `sha256` を持つ `Upload` を作りますが、その
もとに置いてあるものが違います。単一 zip のバインドは、ツリー全体を 1 つのオブジェクトとして
org プレフィックス配下の `uploads/<sha256>.zip` に保存済みです。合成のバインドは成果物ごとに
1 つのオブジェクトを持ち、ツリー全体のオブジェクトを持ちません。

`Upload.worker_ref` は、そのバインドが作ったほうを名指しします。ツリーの `id`、成果物ごとの
sha である `artifacts`（単一 zip のバインドでは None）、そして単一 YAML の `scenarios` 成果物を
置くときの表示名です。1 つの値がどちらの種類も記述するので、以降の単位はバインドの経緯ではなく
この値の中身で分岐します。

合成ツリーを zip 化して単一 zip の形に寄せれば分岐は消えますが、代償が 2 つあります。
合成 1 通りごとにアプリバイナリの複製を持つことになり、BE-0268 の成果物ごとの
重複排除を打ち消します。さらに bind のリクエスト内で数百 MB の zip 圧縮を同期実行します。
成果物を名指しする形なら、その重複排除は保たれ、bind 時の負担もありません。

### 単位 2：その記述をジョブに載せ、リース時に署名する

`_register_and_dispatch` は、すべての dispatcher が通る唯一の末尾です。呼び出し元のバインドから
ジョブの作業ディレクトリを凍結する処理も、すでにここにあります。単位 2 は、同じ場所で
`Upload.worker_ref` を刻みます。新しい `Job.bundle` フィールドが値を保持し、`job_spec` が
直列化するので、記述はキュー経由で Worker へ届きます。

`worker_lease` は、その記述が名指しするオブジェクトごとに presigned GET URL を署名します。単一
zip のバインドでは `bundle` の 1 本、合成の三つ組では成果物の種類ごとに 1 本です。org は Worker
から送られた値ではなく、リース済みジョブから解決し、どの sha もストレージキーになる前に 16 進
ダイジェストとして再検証します。署名した URL は、同じ経路をすでに通っている `baseline_urls` の
隣に `bundle_urls` として並びます。

### 単位 3：Worker 上でバンドルを展開し、そのルートからジョブを走らせる

Worker はリースが署名したものをダウンロードし、Control plane 自身の関数でツリーを組み直します。
単一 zip のバインドは `validate=validate_bundle_config` を添えた `materialize_bundle` を通ります。
この検証が各 target のパスをツリー内へ閉じ込めます（BE-0051）。合成の三つ組は
`materialize_composition` を通ります。bind 自身が実行したのと同じ組み立てで、同じ検証も適用され
ます。続いて `find_bundle_config` がツリーから設定を見つけ、その親ディレクトリがそのジョブの
作業ディレクトリになります。この関数は、すべてを 1 つのフォルダで包んだ zip も許容します。

そのルートから走らせることが、残りを解決させます。設定の相対の `appPath`、`scenarios`、
`baselines` はいずれもツリーの内側を指すので、実行はパスの書き換えなしにそれらを読めます。Worker
のループは、同じルートをコンソールログの書き出しと証跡アップロードにも渡すので、1 つの作業
ディレクトリがジョブ全体をまかないます。

バンドル識別子がツリーの鍵になるので、同じバンドルの 2 本目以降のジョブは何もダウンロードし
ません。したがって 1 つの作業ディレクトリが、そのバンドルのすべてのジョブをまかない、実行は
`runs/` ツリーをその中に書きます。この識別子は内容のダイジェストであってファイル名ではないので、
2 つのビルドは取り違えられません。バイナリのパスが同じで中身が違う 2 つのバンドルは 2 つの
識別子になり、各実行は自分のバイナリを入れます。ランナーの `reinstall` 前提条件は、既定で
その実行の作業ディレクトリからのクリーンインストールです。ジョブごとにツリーを複製すれば
`runs/` の書き込みも分離できますが、そのためだけにアプリバイナリを毎回コピーするのは重すぎます。
ツリーは org ごとに分けます。コントロールプレーンのキャッシュと同じ理由で、ツリーは可変なので、
あるテナントの run の証跡が別のテナントのディレクトリに落ちてはなりません。org の識別子は運用者が
決めるものです。2 つの異なる識別子が同じ安全なセグメントに落ちると、その分離が崩れます。許可外の
文字を含む場合、64 文字を超える場合、大文字小文字だけが違う場合（macOS の既定のように区別しない
ファイルシステム）が該当します。キャッシュは、すでに安全なセグメントである識別子だけをそのまま
使い、それ以外は元の識別子のダイジェストで区別します。

取得は、実行と同じバックグラウンドスレッドで、同じハートビートの下で走ります。ジョブの中で最大の
転送なので、リースのタイムアウトより遅い取得は、Worker が気づかないまま回収を招きます。しかも
Worker はそのままジョブを実行し、再リースを獲得した別の Worker と並走します。

回収に気づくことと、間に合って手を打つことは別です。取得が戻った直後に一度だけ確認するフラグが、
実際に実行の開始を止めます。フラグがなければ、回収の直後に取得が終わったとき、実行用のスレッドは
そのままアプリを導入して実機を操作してしまいます。再リースを獲得した Worker と並走したうえで、
結果だけが後から捨てられます。取得の直後は、まだ実機に何も触れていない唯一の地点です。そこで
早期に戻ってもコストはかかりません。

各パーツは着地後に sha256 を検証します。本文が途中で切れても `http.client` はエラーにしないため
です。検証しなければ、切り詰められた生バイナリは誰にも読まれないまま、壊れたツリーがキャッシュに
残り、そのバンドルの以降のジョブがすべて再利用します。

ダウンロードの失敗とツリー構築の失敗は、例外の型ではなく段階で切り分けます。ダウンロードの失敗
（接続リセット、`503`、ダイジェスト不一致）は何も報告せず、リースを失効させます。キュー自身の
再リースが別の試行を与えるので、1 回のネットワークの揺らぎが赤い run になりません（指針 2）。
ダウンロード後の処理は検証済みのバイトに対して決定的なので、そこでの失敗は恒久的なものとして
報告します。`404` と `410` も恒久的です。オブジェクトは存在せず、再試行しても現れません。型で
切り分けると両方向に誤分類します。`HTTPError` も、壊れたツリーが投げる `FileExistsError` も、
どちらも `OSError` だからです。

運用者が知っておくべき帰結が 2 つあります。1 つは、ツリーを削除する仕組みがないことです。ビルド
ごとにバンドルをアップロードする環境では、ビルドごとに 1 つのツリーが Worker のディスクに積もり
ます。コントロールプレーン自身の展開キャッシュと同じ性質が、ジョブを走らせるマシンの側にも出ます。
本項目で方針を決め切れないので、`docs/self-hosting.md` に `.bundles/` を自分で刈る旨を書きます。
もう 1 つは、バンドルの run が org の保存済みベースラインをダウンロードしなくなることです。
バンドルが自前のものを同梱するためです。serve でベースラインを承認しつつバンドルもアップロード
している環境では、視覚アサーションの比較対象がバンドルの `baselines/` に変わります。

`_get_file` は、応答全体をメモリに読み込まずディスクへストリームします。このヘルパーがもともと
想定していたベースライン画像に比べ、アプリバイナリははるかに大きいためです。

バンドルは自前のベースラインも同梱するので、dispatcher は org 側の保存済みベースラインを
上書きするのをやめる必要があります。`_download_baselines` は書き込み先ディレクトリを先に
消すため、作業ディレクトリがバンドルの内側にある状態では同梱物を削ってしまいます。dispatcher は
アップロードのバインドに対してすでに `--baselines` を省いており、単位 3 は
`materialize_baselines` の側もそれに揃えます。

### 単位 4：テストとドキュメント

ゲートは、ネットワークとシミュレーターのどちらも使わずに各接合部を覆います。

- `worker_lease` が、単一 zip のバインドではツリー全体の zip を、三つ組では成果物ごとに署名します。
- `worker_lease` が、バンドル識別子を持つジョブ仕様に対して `bundle_urls` を返します。
- Worker が、ダウンロードした zip を作業ディレクトリのルートへ解決します。平坦な zip と、包まれた
  zip の両方を試します。
- Worker が三つ組を成果物から組み直し、config が指す `appPath` にバイナリを置きます。
- バイナリのパスが同じで中身が違う 2 つのバンドルが、2 つの作業ディレクトリに解決します。
- バンドル識別子を持たないジョブは、素の作業ディレクトリのままです。Git 由来の実行は影響を
  受けません。
- リースが署名できなかったバンドル識別子を持つジョブは、盲目のまま走らせず失敗します。
- 取得中に届いた回収を検知するので、ハートビートがダウンロードを覆うことを証明します。
- 取得中に見た回収は実行の開始を止め、単なる結果の破棄では終わりません。
- 切り詰め結果が同じになる 2 つの org 識別子でも、衝突しない別々のディレクトリになります。
- 切り詰められたパーツと不一致の成果物ダイジェストを検知し、再利用されるツリーを残しません。
- 取得時の `503` は再試行し、`404` とバイト着地後の失敗は報告します。
- `_get_file` は 1 回の read に収まらない応答をディスクへ書き、バンドルをメモリに溜めません。
- アップロードのバインドは `materialize_baselines` を切って投入し、同梱ベースラインを残します。

`docs/self-hosting.md` と日本語版に、Worker が新たに取得する対象の説明を 1 段落加えます。

## 検討した代替案

| 代替案 | 採用しなかった理由 |
|---|---|
| バイナリを `materials` に入れる | `materials` はパスからテキストへの写像であり、Mach-O バイナリはテキストではありません |
| 合成ツリーを zip 化して 1 つのオブジェクトを署名する | 合成 1 通りごとにアプリバイナリを保存し、BE-0268 の重複排除を打ち消します |
| Worker にオブジェクトストレージの認証情報を渡す | 認証情報を持たない Worker という BE-0160 の主眼を覆します |
| ホスティング対象の全 [target](../../docs/ja/glossary.md#target-app-device) に `build:` を必須にする | ビルド済みバイナリを前提とするアップロードの筋書きが壊れます |
| 分割の両側で共有ストレージをマウントする | HTTP だけで足りている現在の Worker に、インフラ要件を増やします |

成果物を個別に署名すると、Worker が扱う形は 1 つではなく 2 つになります。とはいえ負担は小さく、
どちらも新しいコードではありません。`materialize_bundle` と `materialize_composition` は
Control plane 自身の関数で、Worker はジョブの `Upload.worker_ref` が名指しするほうを呼びます。

## 進捗

> 開発の進行に合わせて常に最新の状態に保ってください。チェックリストは *詳細設計* の MECE な
> 作業分解（作業の単位ごとに 1 つ）に対応し、ログには変更内容と時期（古い順）を PR へのリンクと
> ともに記録します。

- [x] 単位 1 — バインド済みアップロードを `Upload.worker_ref` で 1 度だけ記述します。
- [x] 単位 2 — その記述をジョブに載せ、リース時に `bundle_urls` を署名します。
- [x] 単位 3 — Worker 上でバンドルを組み直し、そのルートからジョブを走らせます。
- [x] 単位 4 — 各接合部のテストと、セルフホスティングのドキュメントを整えます。

## 参考

- [BE-0073 — config・シナリオ・アプリバイナリを zip でまとめてアップロード](../BE-0073-serve-zip-bundle-upload/BE-0073-serve-zip-bundle-upload-ja.md)：本項目がバイナリを届ける、単一 zip のバインドです。
- [BE-0243 — アップロードした zip の config バンドルをオブジェクトストレージへ永続化](../BE-0243-upload-bundle-durable-storage/BE-0243-upload-bundle-durable-storage-ja.md)：本項目が署名する `uploads/<sha256>.zip` というキーです。
- [BE-0268 — config・シナリオ・アプリバイナリを独立した成果物としてアップロード](../BE-0268-composable-upload-artifacts/BE-0268-composable-upload-artifacts-ja.md)：単位 1 が成果物ごとに記述する、合成のバインドです。
- [BE-0160 — presigned URL による Worker の認証情報レスなアップロード](../BE-0160-worker-credential-free-uploads/BE-0160-worker-credential-free-uploads-ja.md)：本項目が拡張する presigned URL の接合部です。
- [BE-0106 — 完了後 Worker モデル](../BE-0106-post-completion-worker-model/BE-0106-post-completion-worker-model-ja.md)：`bundle_urls` フィールドが加わるリースのプロトコルです。
